<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <script src="/js/eventListeners.js" defer></script>
    <title><%= locals.pageTitle %> | <%= locals.appTitle %></title>
  </head>
  <body>
    <%- include("partials/header") %>

    <main>
      <h2>my drive</h2>

      <!-- current path -->
      <div class="row gap-s">
        <b>Location:</b>
        <span class="row">
          <a href="/drive/root" class="underline-hover">root</a>
          <% locals.parentFolders.forEach((parentFolder) => { %> /
          <a href="/drive/<%= parentFolder.id %>" class="underline-hover">
            <%= parentFolder.name %>
          </a>
          <% }) %>
        </span>
      </div>

      <!-- current folder -->
      <div class="row gap-s">
        <b>Folder: </b>
        <span>
          <%= locals.pageTitle %>
        </span>
        —
        <!-- tools -->
        <div class="row gap-s">
          <!-- ============== if not in root ============== -->
          <% if (locals.folderId !== "root") { %>

          <!-- edit -->
          <button
            class="edit-folder-button"
            data-folder-id="<%= locals.folderId %>"
            data-folder-name="<%= locals.pageTitle %>"
          >
            Edit
          </button>

          <!-- delete -->
          <form
            action="/drive/<%= locals.folderId %>/folder/delete"
            method="post"
          >
            <input
              type="text"
              name="currentFolderId"
              value="<%= locals.folderId %>"
              hidden
            />
            <button
              type="submit"
              onclick="
                return confirm(
                  'Are you sure you want to delete &quot;<%= locals.pageTitle %>&quot; and all its contents?',
                );
              "
            >
              Delete
            </button>
          </form>
          <% } %>

          <!-- ============== if in root ============== -->
          <% if (locals.folderId === "root") { %>
          <!-- delete -->
          <form
            action="/drive/<%= locals.folderId %>/folder/delete"
            method="post"
          >
            <input
              type="text"
              name="currentFolderId"
              value="<%= locals.folderId %>"
              hidden
            />
            <button
              type="submit"
              onclick="
                return confirm(
                  'Are you sure you clear your entire drive of all its contents?',
                );
              "
            >
              Clear Drive
            </button>
          </form>

          <% } %>
        </div>
      </div>

      <!-- child folders -->
      <div>
        <h3>Folders</h3>
        <ul>
          <% if (locals.childFolders) { %>

          <% locals.childFolders.forEach((childFolder) => { %>
          <li>
            <div class="row gap-s">
              <a href="/drive/<%= childFolder.id %>">
                <%= childFolder.name %>
              </a>
              —
              <!-- tools -->
              <div class="row gap-s">
                <!-- edit folder -->
                <button
                  class="edit-folder-button"
                  data-folder-id="<%= childFolder.id %>"
                  data-folder-name="<%= childFolder.name %>"
                >
                  Edit
                </button>

                <!-- delete folder -->
                <form
                  action="/drive/<%= childFolder.id %>/folder/delete"
                  method="post"
                >
                  <input
                    type="text"
                    name="currentFolderId"
                    value="<%= locals.folderId %>"
                    hidden
                  />
                  <button
                    type="submit"
                    onclick="
                      return confirm(
                        'Are you sure you want to delete &quot;<%= childFolder.name %>&quot; and all its contents?',
                      );
                    "
                  >
                    Delete
                  </button>
                </form>
              </div>
            </div>
          </li>

          <% }) %>
          <% } %>
        </ul>
      </div>

      <button id="create-folder-button">Add Folder</button>

      <form
        action="/drive/<%= locals.folderId %>/file/create"
        method="post"
        enctype="multipart/form-data"
      >
        <input type="file" name="uploadFiles" multiple required />
        <button type="submit">Add file</button>
      </form>

      <%- include("partials/modals/create-folder") %>
      <%- include("partials/modals/edit-folder") %>
    </main>
  </body>
</html>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    if ("<%= locals.modal %>" === "createFolder") {
      const createFolderModal = document.getElementById("create-folder-modal");
      createFolderModal.style.display = "block";

      createFolderModal.querySelector("input#folderName").focus();
    }
    if ("<%= locals.modal %>" === "editFolder") {
      const editFolderModal = document.getElementById("edit-folder-modal");
      editFolderModal.style.display = "block";

      const editFolderInput = editFolderModal.querySelector("input#folderName");

      editFolderInput.focus();
      editFolderInput.select();
    }
  });
</script>
