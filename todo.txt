now: 
-do not share root folder (google drive also doesnt not share MyDrive)

-check error/no-permit routes
-make/check sharing
-need check file ownership (or shareable)
-delete upload on delete file

-/link /share/token to a folder
  -sub content routes should looks like /share/token/folder/:folderId or /share/token/file/:fileId
  -check "root" shared folder shareTime as first check, then check that the folder/:folderId or file/:fileId is a descendant of the "root" shared folder, then render
  -/share route can reuse folder and file views, just need conditional links and content/tools based on isSharedView local variable


later:
-update to esm?
-owner of folder check
  --and exist check
-addEventListener for drive editFolder popup
  -close and modal styling
    -close should be redirect to curr page to reset modal data
-focus on login input
-store/delete files remotely




for template:
-change default source to gh repos page
-add "change source link to repo in views/partials/header.ejs"
useful cmds:
    -npx prisma migrate dev --name your-migration-name
    -npx prisma generate
    -npx prisma studio --config ./prisma.config.js
    



-remove console.log's
  // =======================================
  // fill demo password
  // =======================================
  function fillDemoPassword() {
    const btn = document.getElementById("demo-password-button");
    const passwordInput = document.getElementById("password");
    const confirmPasswordInput = document.getElementById("confirm-password");

    if (btn) {
      btn.addEventListener("click", (e) => {
        console.log(passwordInput);
        console.log(confirmPasswordInput);